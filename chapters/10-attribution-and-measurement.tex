\chapter{Measurement, Attribution \& Closing the Loop}\label{ch:attribution-and-measurement}

% Scope: the measurement problem (causal inference, not data collection); attribution
%   models (last-/first-click, linear, time-decay, position-based, data-driven Shapley);
%   attribution windows (1/7/28d click, 1d view); multi-touch limits (no counterfactual);
%   media mix modelling (adstock carryover + Hill saturation); incrementality
%   (geo-lift, holdout, conversion lift, iROAS); the iOS 14 / ATT privacy collapse;
%   server-side tracking & the conversion feedback loop (Meta Conversions API + event
%   match quality, Google Enhanced Conversions, offline conversion import); lead signal
%   quality (click < lead < MQL < SQL < opportunity < closed-won).
% Cross-links: data-driven attribution & Bayesian MMM <-> Bayes (\cref{ch:decisions-under-uncertainty});
%   saturation / bid-response <-> elasticity (\cref{ch:microeconomics}); lead signal quality
%   <-> CRM pipeline stages (\cref{ch:digital-funnel-and-crm}); CLV as the value signal fed
%   back to platforms (\cref{ch:customer-economics}).
% Figures: attribution-window flow (TikZ), signal-quality pyramid (TikZ), adstock/saturation curve (matplotlib).
% TODO: draft per the per-concept template; use the \texttt{platform} box for platform mechanics;
%   cite \textcite{meta2024capi}, \textcite{ga4-2024attribution}, etc.
