\chapter{Digital Marketing: Funnel, Lifecycle \& Growth Loops}\label{ch:digital-funnel-and-crm}

% Scope: the funnel as a probability chain (TOFU/MOFU/BOFU); content & SEO (stock vs
% flow asset); email, lifecycle & CRM (lead capture, lead scoring, MQL/SQL, pipeline
% = sum of deal x close-probability); growth loops & flywheels (loop coefficient);
% retargeting & lifecycle-stage segmentation.
% Cross-links: funnel conversion <-> conditional-probability chain (\cref{ch:decisions-under-uncertainty});
%   growth loops <-> network-effect moats (\cref{ch:growth}); funnel throughput <-> Little's Law
%   (\cref{ch:operations}); lifecycle stages <-> behavioural segmentation (\cref{ch:segmentation-targeting-positioning}).
% Figures: funnel (TikZ), pipeline-stage drop-off bars (matplotlib).
% TODO: draft per the per-concept template (decision/intuition/derivation/breaks/worked/trap/deeper).
